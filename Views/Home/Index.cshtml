@{
    ViewData["Title"] = "Home Page";
    string userRoles = ViewBag.userRoles;
}

<div class="container">
    <br>
    <br>
    
    <div class="row">
        
        <div class="col-md-4 d-grid">
            <a class="btn btn-primary btn-lg btn-block" asp-controller="Programs" asp-action="Index">Programmi</a>
        </div>

    @if(userRoles.Contains("root") || userRoles.Contains("MesOperator"))
    {
        <div class="col-md-4 d-grid">
            <a class="btn btn-primary btn-lg btn-block" asp-controller="Mes" asp-action="Main">Mes</a>
        </div>     
    }

        <div class="col-md-4 d-grid">
            <a class="btn btn-primary btn-lg btn-block" asp-controller="Erp" asp-action="Main">Planner</a>
        </div>   

    </div>
</div>
